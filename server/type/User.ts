export interface UserAuth{
    id: string;
    username: string;
    email: string;
    password: string;
    createdAt: Date;
}

export interface UserSend{
    id: string;
    username: string;
    email: string;
    createdAt: Date;
}